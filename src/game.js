import { Gameboard } from "./createGameboard";
import { Player } from "./createPlayer";
import { Ship } from "./createShip";

const Game = () => {
    const you = Player(you, true);
    const comp = Player(computer, false);

    const playerFleet = [
        
    ]
}